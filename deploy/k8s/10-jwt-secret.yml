# ================================
# JWT Secret — 用于 Gateway 和各服务 JWT 验证
# ================================
apiVersion: v1
kind: Secret
metadata:
  name: jwt-secret
  namespace: semantic-atlas
type: Opaque
stringData:
  JWT_SECRET_KEY: cloudys-jwt-k8s-secret-change-in-production
