# ================================
# Gateway Service — 开发环境配置
# ================================
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8888/eureka

jwt:
  secret-key: cloudys-jwt-dev-secret-key-change-in-production
  expiry-hours: 24
